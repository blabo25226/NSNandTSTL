# AGENTS.md — NSN × TSTL 統合アルゴリズム開発

このリポジトリは、2本の論文で提案された手法を組み合わせた新アルゴリズムの研究・実装を行う。エージェントは本ファイルを最初に読み、作業の文脈・制約・ディレクトリ規約に従うこと。

---

## プロジェクト概要

| 略称 | 論文 | arXiv | ローカル PDF |
|------|------|-------|--------------|
| **NSN** | Hardware-Efficient Neuro-Symbolic Networks with the Exp-Minus-Log Operator (Ipek, 2026) | [2604.13871](https://arxiv.org/abs/2604.13871) | `NSN/docs/2604.13871v1.pdf` |
| **TSTL** | Is One Layer Enough? Training a Single Transformer Layer Can Match Full-Parameter RL Training (Zhang et al., 2026) | [2607.01232](https://arxiv.org/abs/2607.01232) | `TSTL/docs/2607.01232v2.pdf` |

**最終目標**: NSN の neuro-symbolic 表現力と TSTL の層選択的学習を統合したアルゴリズムを設計・実装・評価する。

---

## 論文サマリ（エージェント向け）

### NSN — DNN-EML ハイブリッド

- **中核演算子**: `eml(x, y) = exp(x) - ln(y)`（Exp-Minus-Log, EML）。定数 `1` と組み合わせれば全ての初等関数を表現可能（Sheffer 型完全性）。
- **アーキテクチャ**:
  1. **Stage 1 (trunk)**: 標準 MLP が入力 `x` から低次元特徴 `z ∈ R^d` を抽出。
  2. **Stage 2 (head)**: 深さ `D` の二分 EML 木が `z` から予測 `ŷ` を生成。各葉は `ℓ_i(z) = α_i + β_i^T z + γ_i f_prev`。
- **解釈可能性**: 葉重みを softmax → hardening → `{0,1}` にスナップし、閉形式の初等関数式を復元。
- **制約・注意点**:
  - 複素数演算が必要（実数ターゲットでも内部は `C`）。
  - `exp` オーバーフロー・`ln` 特異点により数値的に脆弱。クランプ必須。
  - 深さ `D ≥ 5` では学習成功率が急落（`D ≤ 4` 推奨）。
  - CPU/GPU ではノードあたりコストが高い（≈111 FLOPs/node）。専用 EML セル（FPGA/アナログ）向け。
- **実装ロードマップ（論文提案）**: PyTorch `EMLNode`、Feynman DB ベンチマーク、FPGA 合成。

### TSTL — 単一層 RL 適応と層寄与

- **問題設定**: LLM の RL 後学習（GRPO 等）において、全パラメータ更新が最適かを層単位で検証。
- **層寄与 (layer contribution)**:
  ```
  C(k) = (S_k - S_base) / (S_full - S_base)
  ```
  - `S_k`: 層 `k` のみ学習したときの性能
  - `S_base`: ベースモデル、`S_full`: 全パラメータ RL 後
  - `C(k) = 1.0` で全パラメータ RL と同等、`> 1.0` でそれを上回る
- **主要知見**:
  - 最良の単一層だけで全パラメータ RL の大部分〜全量を回復可能（場合により上回る）。
  - 高寄与層は **ネットワーク深さの 40–60%（中間層）** に集中。入出力端は寄与が低い。
  - 層ランキングはデータセット・タスク・RL アルゴリズム間で強い相関（Spearman ρ ≈ 0.6–0.8）。
  - 全パラメータ学習時の重み変化量は層間でほぼ均一 → 寄与は「変化量」ではなく「パラメータ部分空間の有効性」に由来。
- **実用的戦略**:
  1. **Boost Bk**: 高寄与 k 層の学習率を引き上げ。
  2. **Only Bk**: 高寄与 k 層のみ学習（他を凍結）。
  3. **Mid-k ヒューリスティック**: プロファイリング不要で中間 k 層を選択。
  4. **アンサンブル**: 異なる高寄与層で学習したモデルは補完的 → majority voting で追加 gain。

---

## 統合の研究仮説（初期方向）

NSN と TSTL はドメインが異なる（回帰/分類の neuro-symbolic vs LLM RL）が、以下の接点がある。

| NSN の要素 | TSTL の要素 | 統合の可能性 |
|------------|-------------|--------------|
| MLP trunk（多層） | 中間層への寄与集中 | trunk の高寄与層のみ学習し、EML head は浅く保つ |
| EML head（浅い木） | 単一層で大部分を回復 | head 全体 vs trunk 各層の寄与プロファイリング |
| 記号的スナップ | 層選択的学習 | スナップ前は trunk のみ更新、スナップ後は head 重みを固定 |
| 計算コスト削減（エッジ向け） | 凍結による学習効率化 | 低寄与層を凍結して学習・推論コストを削減 |

エージェントは上記を出発点としつつ、実験結果に基づいて仮説を更新すること。統合設計の変更は `docs/` または `texts/` に記録する。

---

## ディレクトリ構成

```
NSNandTSTL/
├── AGENTS.md          # 本ファイル（エージェント向けプロジェクト指示）
├── README.md          # 人間向けプロジェクト概要（大きなコーディング完了後にまとめて書く）
├── source.md          # 参考文献リンク集（ユーザーが管理。エージェントは編集しない）
├── daily_report.md    # 作業ログ（エージェントが作業のたびに時系列で追記）
├── .gitignore         # data/, docs/ を除外
│
├── src/               # 統合アルゴリズムの実装（メインコード）
├── data/              # 共通データセット（git 管理外）
├── docs/              # 統合研究の設計書・メモ（git 管理外）
├── results/           # 実験結果・ログ・図表
├── texts/             # 論文ノート・考察ドラフト
│
├── NSN/               # NSN 単独の再現・実験
│   ├── src/
│   ├── data/
│   ├── docs/          # 2604.13871v1.pdf
│   └── texts/
│
└── TSTL/              # TSTL 単独の再現・実験
    ├── src/
    ├── data/
    ├── docs/          # 2607.01232v2.pdf
    └── texts/
```

### 配置ルール

- **統合コード** → ルート `src/`。サブモジュール化する場合は `src/nsn/`, `src/tstl/`, `src/hybrid/` など。
- **NSN 単独の再現** → `NSN/src/`。TSTL 単独 → `TSTL/src/`。
- **実験出力** → `results/`（サブフォルダで実験名・日付を区別）。
- **設計判断・メモ** → `docs/` または `texts/`。
- **論文 PDF** は各 `*/docs/` に配置済み。`.gitignore` によりリポジトリには含まれない。

---

## コーディング規約

1. **最小スコープ**: 依頼された変更のみ。無関係なリファクタリング禁止。
2. **既存慣習に従う**: 新規ファイル追加前に周辺コードのスタイルを確認。
3. **Python / PyTorch 想定**: NSN は PyTorch `EMLNode`、TSTL は veRL / GRPO 系。統合実装も PyTorch を第一候補とする。
4. **数値安定性（NSN）**: `exp` クランプ、`ln` の定義域チェック、複素数オートグラッドの明示的処理。
5. **再現性**: シード固定、ハイパーパラメータをコードまたは設定ファイルに明記。結果は `results/` に保存。
6. **コメント**: 非自明な数式・ハイパーパラメータ選択理由のみ。自明な説明は不要。
7. **テスト**: 明示的な依頼または重要な数値ロジック（EML 順伝播・逆伝播、層寄与計算）に限定して追加。

---

## エージェント作業フロー

1. **文脈確認**: 本ファイル → 関連する `NSN/docs/` または `TSTL/docs/` の PDF → 既存 `src/` コード。
2. **スコープ確認**: 統合 (`src/`) か、単独再現 (`NSN/`, `TSTL/`) かをユーザー意図に合わせる。
3. **実装前**: 変更対象ディレクトリの既存ファイルを読む。空ディレクトリの場合は論文に基づき初期構成を提案してよい。
4. **実装後**: 該当コードの動作確認。可能なら最小のスモークテストを実行。
5. **記録**: 何か作業をしたら必ず `daily_report.md` に時系列で追記する（後述）。
6. **Git**: コミット・プッシュはユーザー明示時のみ。

---

## 主要な技術用語

| 用語 | 意味 |
|------|------|
| EML / eml | `exp(x) - ln(y)`。NSN の Sheffer 型演算子 |
| DNN-EML | MLP trunk + EML tree head のハイブリッド（NSN） |
| Layer contribution C(k) | 層 k 単独学習が全パラメータ RL 改善の何割を回復するか（TSTL） |
| GRPO | Group Relative Policy Optimization（TSTL の主要 RL アルゴリズム） |
| Symbolic snapping | 学習後に重みを `{0,1}` 頂点にクランプし閉形式式を得る（NSN） |
| Only Bk / Boost Bk | 高寄与 k 層のみ学習 / 高寄与 k 層の学習率ブースト（TSTL） |

---

## ルート直下ドキュメントの管理

| ファイル | 管理者 | ルール |
|----------|--------|--------|
| `README.md` | ユーザー | **大きなコーディングが終わったらまとめて書く**。エージェントは依頼がない限り編集しない。 |
| `source.md` | ユーザー | **リンクを貼り付ける場所**。参考文献・外部 URL をユーザーが管理する。エージェントは編集しない。 |
| `daily_report.md` | エージェント | **作業のたびに時系列で追記する**（下記フォーマット参照）。 |

### `daily_report.md` 追記フォーマット

エージェントがコード変更・調査・設計・ドキュメント更新など何かを行ったら、ファイル末尾に以下の形式で追記する。

```markdown
## YYYY-MM-DD HH:MM

- **作業内容**: （何をしたか、1〜3 行）
- **変更ファイル**: （パスを列挙、なければ「なし」）
- **メモ**: （次のステップや判断理由があれば）
```

日付は作業時点のローカル時刻を用いる。同一会話内で複数回作業した場合も、まとめて 1 エントリにしてよい。

---

## 禁止事項

- `.gitignore` で除外された `data/`・`docs/` 内の大容量バイナリをコミットしない。
- 論文 PDF の内容を要約せずに数式・アルゴリズムを改変しない。不確かな場合は PDF を再確認。
- 統合アルゴリズムの最終設計を独断で確定しない。実験とユーザー合意を経る。
